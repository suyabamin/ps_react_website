import { apiFetch } from "../utils/fetch.js";

/**
 * Stub implementation for Expense Tracker – UI removed per Lead Architect.
 * Provides a no‑op `init` to keep module importable without side effects.
 */
export function init() {
  // No UI rendering.
}
